import React from 'react';
import {Pressable} from 'react-native';

import {Container} from './styles';

export const Button = () => {
  return (
    <Container>
      <Pressable />
    </Container>
  );
};
